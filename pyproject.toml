[project]
name = "developer-opportunity-monitor"
version = "0.1.0"
description = "Local-first monitor for engineering roles with Telegram alerts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "requests==2.32.3",
  "pyyaml==6.0.2",
  "python-dotenv==1.0.1",
]

[tool.uv]
package = false
